name: flutter_touch_scale
description: "This Flutter package delivers clear visual feedback through scale animations on user touch interactions."
version: 1.6.1
repository: https://github.com/MTtankkeo/flutter_touch_scale

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
